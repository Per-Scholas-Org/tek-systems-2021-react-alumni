/* console.log("hi");

const array: Array<string> = ["a","b","c","d","e","f","g"];

array.forEach((element, index) => {
    console.log("array index " + index + " has element " + element);
}); */

class MyStack{
    constructor(first,last) {
        this.first = null;
        this.last = null;
        this.size = 0;
        const array: [];
    }


    push: (item:any ) => this.data.push(item);


    pop: null;
}
